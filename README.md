# chdwilsonPrism
This function calls the CHDWILSON model to predict coronary heart disease (CHD) over 10 years using TChol and LDL categories. 
To get the default inputs: sample_input <- get_default_input()
To run the model with the default inputs: model_run (sample_input)
